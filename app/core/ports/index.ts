/**
 * Ports — interfaces defined by core and implemented by adapters.
 * Placeholder: the real signatures (Repository, PriceProvider, FxProvider,
 * ImportAdapter, TaxJurisdiction) are added in the core step.
 */
export {};
